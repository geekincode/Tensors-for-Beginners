2、张量定义
===================================

张量是一个多维数组，可以包含任意数量的维度。它是深度学习中最基本的数据结构，用于表示输入数据、模型参数和输出结果。

在Python中，我们可以使用NumPy库来定义张量。以下是一个简单的例子：


.. math::
  \begin{bmatrix}
    1 & 2 & 3 \\
    4 & 5 & 6 \\
    7 & 8 & 9
  \end{bmatrix}

  